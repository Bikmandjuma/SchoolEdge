{
    "private": true,
    "type": "commonjs", // Change to commonjs to avoid ES module issues
    "scripts": {
        "dev": "npm run development",
        "watch": "npm run watch",
        "build": "npm run production",
        "development": "mix",
        "production": "mix --production"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.19",
        "axios": "^1.6.4",
        "laravel-mix": "^6.0.49",
        "mini-css-extract-plugin": "^2.5.0", // Add this if using CSS extraction
        "postcss": "^8.4.39",
        "postcss-loader": "^6.2.1", // Ensure this is added
        "tailwindcss": "^3.4.4",
        "webpack": "^5.0.0", // Add this if missing
        "webpack-cli": "^4.0.0" // Add this if missing
    },
    "dependencies": {
        "laravel-echo": "^1.16.1",
        "pusher-js": "^8.4.0-rc2",
        "toastr": "^2.1.4"
    }
}
